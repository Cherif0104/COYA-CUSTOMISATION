# Workflows — Documents

## WF-DOC-APPROVAL

Upload → scan → validation rôle métier → `active` + événement `Document.Approved`.

## WF-DOC-RETENTION

Archivage programmé selon politique `core` / conformité.
