# Workflows — Analytics

## WF-ANL-ALERT

`AlertRule.Triggered` → notification → option création tâche (`projects`) ou ticket (`tech`).

## WF-ANL-EXPORT

Demande export → validation droit → génération asynchrone → lien sécurisé (`documents` ou storage).
