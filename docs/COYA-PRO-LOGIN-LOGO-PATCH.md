# Correctif Login coya-pro : remplacer "COYA SENEGEL" par le logo

**Modification déjà appliquée** dans `coya-pro/components/Login.tsx` : le bloc texte + icône a été remplacé par l’image du logo.

## Logo à afficher

- Placez le fichier **`logo_senegel.png`** dans **`coya-pro/public/`**.
- Source possible : image fournie (nom `coya-pro_logo_senegel.png`) ou `COYA CUSTOMISATION/assets/...` → copiez-la dans `coya-pro/public/logo_senegel.png`.

Sans ce fichier, la page Login affichera une image cassée. Voir aussi `coya-pro/public/README-logo.md`.
