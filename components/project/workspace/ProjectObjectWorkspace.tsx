/**
 * Workspace objet projet — route `/projects/:id` + shell MAKE FIGMA.
 * Implémentation : `ProjectDetailPage.tsx` (extraction progressive des sous-modules).
 */
export { default } from '../../ProjectDetailPage';
export { ProjectObjectWorkspace, ProjectObjectWorkspace as ProjectDetailPage } from '../../ProjectDetailPage';
